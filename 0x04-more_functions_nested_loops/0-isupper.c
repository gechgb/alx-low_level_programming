#include "main.h"
/**
* _isupper: prints 1 if it is uppercase else 0
* @C: character to chake 
*
* return: (0 , 1)
*/
int _isupper(int c)
{
	char c = A;
	if (_isupper(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}

